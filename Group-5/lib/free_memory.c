int free_memory(){
	int output = -1;
	//Place associated block on the free list
	//If new freed block is adjacent to other free blocks, they must all merge
	//Parameters: Pointer to the start address of an allocated block
	//Returns: 0 on success | anything on error(could use for debugging)
}
